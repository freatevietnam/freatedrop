# Freate Drop local settings
# Copy this file to local_settings.py and adjust values.

SECRET_KEY = "change-me-in-production"

DEBUG = False

ALLOWED_HOSTS = [
    "127.0.0.1",
    "localhost",
    "drop.example.com",
]

FERNET_KEY = "generate-with-python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\""

# Database — SQLite by default
# DATABASE_ENGINE = "postgresql"  # or "mariadb"
# DATABASE_NAME = "freatedrop"
# DATABASE_USER = "freatedrop"
# DATABASE_PASSWORD = "sekret"
# DATABASE_HOST = "127.0.0.1"
# DATABASE_PORT = "5432"

CSRF_TRUSTED_ORIGINS = [
    "http://127.0.0.1:8000",
    "http://localhost:8000",
    "https://drop.example.com",
]
